# Studio to Agent Harness durable approval binding TDD evidence

- Date: 2026-09-01
- Base revision: `3ee12be589919d4fe88e5367664485a1f8147caa`
- Scope: private backend-only durable approval composition
- Mode: strict RED -> GREEN -> TRIANGULATE -> REFACTOR

## TDD cycles

| Task | Test | RED | GREEN | Triangulation / safety |
|---|---|---|---|---|
| Real authority composition | `test_durable_director_authority_prepares_approves_and_executes` | Exact test errored with `approval_authority_invalid` at kernel construction. | Exact test passed after adding the narrow port and explicit Studio implementation. | Real `StudioStore`, authenticated authority, `AgentEventLog`, kernel, coordinator, and immutable fingerprint. |
| Pre-journal authorization | `test_authority_snapshot_is_checked_immediately_before_and_after_journal_begin` | Observed `['begin', 'check', 'check']` instead of the required prefix. | A direct boundary probe now observes `['check', 'begin', 'check']`; it does not infer order from clock-read counts. | Retains post-begin and live checks so revocation cannot use the precheck race window. |
| Authority provenance and output custody | `test_forged_direct_authority_cannot_select_approval_evidence_or_execute` | A caller-defined direct port subclass reached one journal begin and one provider request. | The same attack is rejected at construction with zero journal, provider, or tool effects. | Weak exact-instance registration; constructor-bound registration; copy/deepcopy where available; subclass; `object.__new__`; owner registry swap; Studio copy; instance/class method replacement; post-begin method swap. |
| Studio construction provenance | `test_direct_studio_constructor_cannot_seed_approval_chain_without_passphrase` | A direct Studio constructor using persisted credential evidence and an arbitrary event key prepared, approved, began one journal, and reached one provider request. | The public constructor, guessed/reused/rebound completion, and direct provisional helper all reject; only fully audited enroll/unlock completion can consume the exact one-time capability and register. | Capability binds exact instance, Store, credential, and event key objects and is absent from authority state and public bytes. |
| Construction capability identity and retirement | `test_studio_capability_rejects_equal_nonidentical_construction_values`, `test_studio_capability_is_one_shot_and_retires_after_registration_failure` | These focused tests were added as direct regression coverage after the earlier capability implementation; no new independent RED is claimed. | Direct isolated-capsule calls prove that equal but nonidentical credential and event-key objects reject, successful consumption is one-shot, and a registration exception retires the exact pending token without registering or returning its authority. | The older dispatch-substitution tests remain closure-capture coverage; they are not presented as direct credential/key/token evidence. |
| Registered construction-state custody | `test_registered_studio_construction_state_cannot_be_rebound` | Replacing a registered Studio authority's event key on an empty chain still allowed prepare, approval, one journal begin, and one provider request. | Closure custody now seals exact construction-state object identity and rejects the replacement at kernel construction with zero effects. | Triangulates Store, credential, connection, and lock swaps plus in-memory records/lock replacement while retaining weak authority lifecycle. |
| Closure-owned custody capsule | `test_direct_registry_insertion_cannot_authorize_forged_instance` | Direct mutation of the module registry admitted a forged authority, one journal begin, and one provider request. | No mutable registry mapping is exposed; adding a same-named module attribute cannot affect admission and the forged authority has zero effects. | Synchronized closure state, exact registration callers/classes/functions, weak retirement, direct registration failure, and owner-swap resistance. |
| Immutable trust-seam capture | `test_mutable_in_memory_admission_constants_cannot_register_exact_forgery`, `test_mutable_function_derivation_cannot_admit_replaced_exact_class_methods`, `test_mutable_studio_provenance_consumer_cannot_register_a_copy`, `test_mutable_registry_validator_cannot_admit_an_unregistered_copy` | The first seven-selector RED reported six failures and one Studio reviewer-guard error. Constants, function derivation, and unregistered-copy attacks each reached `journal=1/provider=1`; after correcting the Studio reviewer input, its copied authority also reached `journal=1/provider=1`. | Constructor/function identities, function derivation, Studio type/functions/consumer, registry validation, and binding construction now use closure-captured originals; replacing same-named module globals cannot admit an authority. | Covers exact-class forgery, replaced canonical method, Studio copy, and unregistered in-memory copy with zero fingerprint, journal, provider, or tool effects after GREEN. |
| Immutable live and Studio completion validation | `test_mutable_binding_validator_cannot_restore_revoked_state_owner`, `test_mutable_binding_validate_method_cannot_restore_revoked_state_owner`, `test_mutable_studio_registration_completion_cannot_return_unregistered_authority`, `test_mutable_studio_completion_dispatch_cannot_return_unregistered_authority`, `test_mutable_studio_provisional_dispatch_cannot_substitute_a_copy` | Both live validator replacements restored an approved state owner and executed; registration/completion replacements returned an unregistered authority; provisional replacement substituted an arbitrary exact instance. | Binding validation is closure-owned and captured by the kernel; Studio construction entrypoints immutably retain the original provisional/completion/registration closures and return only successfully registered authorities. | Before- and after-kernel binding replacement are rejected; a real post-consumption registration failure returns no authority, invalidates the Store, and a fresh reopen/unlock registers through a new one-time capability. |
| Capsule-owned kernel validation | `test_kernel_approval_validation_handle_cannot_be_reassigned`, `test_replaced_binder_alias_before_kernel_construction_cannot_admit_copy` | A separately assignable kernel validation callback plus a genuinely deep-copied pre-revocation record owner executed after revoke; replacing the imported binder before construction admitted an unregistered copy and reached `journal=1/provider=1`. | Kernel construction closes over the exact capsule binder. The frozen exact binding owns its binding-identity/owner validation closure; the obsolete kernel callback name rejects assignment. Both attacks now reject with zero journal, provider, or tool effects. | The restored approved record is structurally independent, not a shallow dictionary containing the already-revoked record object. |
| Captured custody and Studio construction primitives | `test_execution_authority_capsule_captures_registration_primitives`, `test_execution_authority_capsule_captures_binding_primitives`, `test_mutable_frame_accessor_cannot_register_forged_in_memory_authority`, `test_mutable_studio_frame_accessor_cannot_complete_copied_authority`, `test_mutable_studio_frame_accessor_cannot_create_provisional_authority`, `test_mutable_studio_bound_entrypoint_factory_cannot_return_copy` | The first nine-selector run recorded the live validator/binder/frame vulnerabilities above plus dynamic primitive failures; one first-pass descriptor fixture recursed and was corrected before it was retained. | Exact frame access, weak-reference/identity/type/object operations, predicates, namespace lookup, function/method/binding constructors, Studio token primitives, and the descriptor's bound factory are captured. Ordinary same-named module rebinding cannot forge registration or substitute a returned authority. | `test_studio_construction_capsule_captures_identity_and_token_factory` triangulates Studio `id`/`object` rebinding. This is ordinary imported-module hardening, not reflective-interpreter isolation. |
| Nested snapshot-helper custody | `test_studio_snapshot_helpers_replaced_after_construction_fail_before_effects` | The focused regression produced four failures because neither late instance nor class replacement raised `KernelError`; both `_snapshot_in_transaction` and its nested `_row` accessor remained live call targets. | The exact regression passes after authority custody captures and revalidates every exact class-defined function while the binding still exposes only the three existing port methods. | Four subtests cover instance/class replacement of both helpers. Each replacement is rejected before the replacement callback, journal begin, provider request, or tool call. |
| Exact output binding | `test_exact_snapshot_and_check_outputs_bind_to_minted_review_and_decision` | The forged-authority RED demonstrated that structurally valid attacker-selected evidence reached fingerprinting and execution. | Exact review/snapshot/check cross-binding rejects other review hashes, decision hashes, tools, state, and expiry. | Fingerprint v3 can only consume hashes from the exact kernel-minted review and its frozen decision snapshot. |
| Durable replay and drift | Remaining bridge tests | Added after the behavior-specific RED cycles as adversarial triangulation; no independent RED is claimed. | Focused bridge module: 46 tests, `OK`. | Covers constructor/method/state/trust-seam sealing, reopen/unlock, independent secondary-connection revocation during begin, terminal replay after revoke, zero new effects, private EventLog byte absence, stale request drift, and no late-decision adoption. |

## Final bounded gates

- Bridge: 46 tests, `OK`.
- Explicit non-native approval classes: 21 tests, `OK`.
- Studio authenticated authority: 72 tests, `OK`.
- Kernel and EventLog: 113 tests, `OK`.
- Harness contracts and Studio protocol v6: 36 tests, `OK`.
- Runtime import audit: `OK`, `ai_imports=0`.
- Four changed Python files compiled below
  `/tmp/world-forge-studio-bridge-compile-helper-fix`; the protected public-byte
  diff and `git diff --check` passed.
- Ruff is unavailable. The identity generator was run only in check mode and
  passed with `306` entries and `1072` occurrences; no generated source was
  written.

## Evidence boundaries

Provider and tool objects are deterministic Harness test adapters only; they do
not constitute provider, native, service, socket, or hosted evidence. No
spawn-process revocation test is claimed. Cross-connection and restart
visibility use the real durable authority's transaction audits and focused
Store authority suite. No generated source was written.
